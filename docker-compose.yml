version: '3.7'
services:
  mongodb_container:
    image: mongo:latest

    environment:
      MONGO_URL: mongodb+srv://saucekode:softbadass832@fotosplash.715ur.mongodb.net/test

    ports:
      - 27017:27017
    volumes:
      - mongodb_data_container:/data/db

volumes:
  mongodb_data_container: